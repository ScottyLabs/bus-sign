[project]
name = "bus-sign"
revision = "1.0"

[profiles.default]
PRT_API_KEY = { description = "Pittsburgh Regional Transit API key", required = true }

[profiles.dev]
HOST = { description = "Backend bind address", default = "127.0.0.1" }
PORT = { description = "Backend listen port", default = "8080" }
VITE_API_BASE = { description = "Frontend API base URL for dev server", default = "http://localhost:8080" }
